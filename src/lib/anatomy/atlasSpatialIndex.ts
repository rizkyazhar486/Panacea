/**
 * Reserved boundary for a future deterministic whole-body spatial index.
 *
 * The previous revision contained only an executable `placeholder` identifier,
 * which broke TypeScript builds. Keep this module intentionally inert until a
 * real spatial-index implementation, deterministic tests, and performance
 * acceptance coverage land together. Do not expose a fake BVH API meanwhile.
 */
export {}
